server:
  port: 8081

spring:
  profiles:
    active: dev #prod #default
  jackson:
    deserialization:
      fail-on-unknown-properties: true
 # Keycloak Server Configurations
keycloak:
  realm: ${KEYCLOAK_REALM_NAME:saas-realm}
  credentials: 
    secret: ${KEYCLOAK_CLIENT_SECRET:Fn7lKppwFB7J9kCWD6g9cuF6pSvfEAdk}
  resource: ${KEYCLOAK_CLIENT_NAME:saas-client}
  auth-server-url: http://${KEYCLOAK_CONTAINER_NAME:localhost}:8080/auth
  ssl-required: external
  public-client: false
  use-resource-role-mappings: true
  
#================================================================
#Keycloack UserName and Password For Ingress Token
base-ingress-ms-url: http://localhost:8081
token-username: demo
token-password : demo
token-url: /user/token
keycloak-url:
  realm: /admin/realms
keycloak-config:
  client-id: saas-client
  tenant-id-1: 1d68fd1b-7f05-4856-b268-7b19cd572dab