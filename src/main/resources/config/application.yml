server:
  port: 8081

spring:
  profiles:
    active: dev #prod #default
  jackson:
    deserialization:
      fail-on-unknown-properties: true
 # Keycloak Server Configurations
keycloak:
  realm: ${KEYCLOAK_REALM_NAME:Yash_realm}
  credentials: 
    secret: ${KEYCLOAK_CLIENT_SECRET:Morx0QCWDTSQhT6WXzXU6b4puBG50tPt}
  resource: ${KEYCLOAK_CLIENT_NAME:search-as-service}
  auth-server-url: http://${KEYCLOAK_CONTAINER_NAME:localhost}:8080/auth
  ssl-required: external
  public-client: false
  use-resource-role-mappings: true