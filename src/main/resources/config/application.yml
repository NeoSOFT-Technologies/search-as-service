server:
  port: 8081

spring:
  profiles:
    active: dev #prod #default
  jackson:
    deserialization:
      fail-on-unknown-properties: true
 # Keycloak Server Configurations
keycloak:
  realm: ravi_realm
  credentials: 
    secret: oFN5MkLnp7hLRgojjZ3nJWJDGR31muLg
  resource: search-as-service
  auth-server-url: http://localhost:8080/auth
  ssl-required: external
  public-client: false
  use-resource-role-mappings: true